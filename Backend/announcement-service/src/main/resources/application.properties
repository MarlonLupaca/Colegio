spring.application.name=announcement-service
server.port=8085

spring.datasource.url=jdbc:postgresql://localhost:5433/clg_announcement
spring.datasource.username=colegio-postgres
spring.datasource.password=Yamada

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Disable database auto-configurations for local development testing without running DBs

# Disable Eureka registration for testing individual microservices standalone
eureka.client.enabled=false

